# ignore-scripts=true is set globally in ~/.npmrc
# Postinstall hooks are blocked for ALL packages.
# To rebuild packages needing native compilation, run:
#   npm rebuild puppeteer esbuild better-sqlite3 unrs-resolver
